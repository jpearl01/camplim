# does this copy the Form template and add its profiles?
# what does it get from its Template?

# bootstrap-input can avoid copying the default input as well.
# instead, add its profile.

# so, we use the HTML provided by the bootstrap package.
# and, we use the profiles from Form for all the usual stuff.
